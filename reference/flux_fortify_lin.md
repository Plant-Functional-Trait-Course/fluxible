# Fortify fluxes with linear fit

Extracts fluxes that were fitted with a linear model

## Usage

``` r
flux_fortify_lin(slopes_df, f_datetime)
```

## Arguments

- slopes_df:

  dataset containing slopes

- f_datetime:

  column with datetime of each data point
